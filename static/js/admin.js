/**
 * StroyControl — скрипты для админ-панели
 * В текущей версии основные действия выполняются через формы.
 * Здесь можно добавить расширенную логику при необходимости.
 */

(function () {
    'use strict';

    // Пример: подтверждение удаления (уже есть onsubmit в HTML)
    document.querySelectorAll('form[onsubmit]').forEach(function (form) {
        // Дополнительная логика при необходимости
    });
})();
